{
  "name": "OpenCode",
  "launch_command": "opencode",
  "status_source": "tmux",
  "_comment": [
    "No first-class hook system assumed, so status comes from tmux pane",
    "scraping (see docs/product-spec.md section 5 - this is the brittle path",
    "and where per-harness maintenance lands when the TUI changes).",
    "Verify the prompt patterns below against your actual OpenCode version."
  ],
  "tmux_scrape": {
    "poll_seconds": 1.0,
    "blocked_patterns": [
      "\\(y/n\\)",
      "Allow\\?",
      "Approve",
      "\\[y/N\\]"
    ],
    "idle_patterns": [
      "^>\\s*$",
      "Ready"
    ]
  },
  "actions": {
    "KEY_ENTER": {
      "tap": {
        "type": "keystroke",
        "keys": [
          "return"
        ]
      },
      "hold": {
        "type": "none"
      }
    }
  },
  "palette": [
    "/new",
    "/clear",
    "run the tests",
    "review this diff",
    "explain this error"
  ],
  "_bridge_handled": [
    "KEY_AGENT1-4: tap = summon that tmux session fullscreen, hold = interrupt it",
    "KEY_TERM: tap = launch launch_command here, double-tap = launch in a split, hold = new window",
    "KEY_MIC: tap = send Space (voice-mode toggle in the chat prompt)",
    "KEY_CANCEL: tap = send Escape (deny prompt / interrupt)",
    "KEY_ENTER: approve when a request is pending, hold = sessions overview"
  ]
}
